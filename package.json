{
  "devDependencies": {
    "cypress": "^14.5.3"
  },
  "scripts": {
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "test": "npm run start && npx cypress run --headless && npm run stop"
  }
}
